{
	"name": "angular-controllers-data-sharing",
	"version": "0.0.0",
	"description": "Options for sharing data between AngularJS controllers",
	"repository": "https://github.com/crudbetter/angular-controllers-data-sharing",
	"license": "MIT",
	"devDependencies": {
		"bower": "~1.3.9",
		"http-server": "~0.6.1"
	},
	"scripts": {
		"postinstall": "bower install",

		"prestart": "npm install",
		"start": "http-server -a localhost -p 8000 -c-1"
	}
}
